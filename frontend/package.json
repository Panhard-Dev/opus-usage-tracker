{
  "name": "ia-tracker-frontend",
  "version": "1.0.0",
  "description": "UI do IA Tracker (dashboard + admin de API keys).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.39.0",
    "vite": "^6.4.2",
    "wrangler": "^4.95.0"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2"
  }
}
